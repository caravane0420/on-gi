# ================================================================
# 온기(On-gi) — Vite(client) + Fastify/Socket.io(server) monorepo
# ================================================================

# ── Dependencies ──
node_modules/
client/node_modules/
server/node_modules/

# ── Environment ──
.env
.env.local
.env.*.local
client/.env
client/.env.*.local
server/.env

# ── Build output ──
dist/
build/
client/dist/
server/dist/

# ── OS / IDE ──
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# ── Debug ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Legacy Vercel build artifacts (Next.js — being rolled back) ──
.next/
out/
